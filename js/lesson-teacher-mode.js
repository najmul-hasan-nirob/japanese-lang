// =====================================================
// Lessons — Teacher Mode
// Japanese → 5 second pause → Bangla → next card immediately.
// Cross-browser / Android WebView voice handling.
// =====================================================
(function () {
    const WAIT_MS = 5000, VOICE_WAIT_MS = 6000, RETRY_MS = 500;
    let state='stopped',cards=[],index=0,pausedPhase=null,waitTimer=null,waitStartedAt=0,waitRemaining=WAIT_MS,pausedSpeech=false,runId=0;
    const ICONS={play:'<svg class="lesson-control-svg" viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5l11 7-11 7z"></path></svg>',pause:'<svg class="lesson-control-svg" viewBox="0 0 24 24" aria-hidden="true"><path d="M7 5v14M17 5v14"></path></svg>',stop:'<svg class="lesson-control-svg" viewBox="0 0 24 24" aria-hidden="true"><rect x="6" y="6" width="12" height="12" rx="1"></rect></svg>'};
    const labelIcon=(label,icon)=>'<span class="lesson-control-text">'+label+'</span>'+icon;
    const synth=()=>window.speechSynthesis||null;
    const getVoices=()=>synth()?synth().getVoices():[];
    function getCurrentCards(){return Array.from(document.querySelectorAll('#grid > .card')).filter(c=>{const s=getComputedStyle(c);return s.display!=='none'&&c.offsetParent!==null;});}
    function japaneseText(card){const clone=(card.querySelector('.front')||card).cloneNode(true);clone.querySelectorAll('.lesson-card-topbar,.speaker-btn,.speak-btn,.pronunciation-btn,.romaji,.hard-star,.lesson-card-number,img,svg,button').forEach(e=>e.remove());return clone.textContent.replace(/[^\u3040-\u30ff\u3400-\u9fff\uff66-\uff9fー々〆〇・「」『』【】［］（）！？。、「」\s]/g,'').replace(/\s+/g,' ').trim();}
    function banglaText(card){const el=card.querySelector('.bangla');return el?el.textContent.replace(/\s*\/\s*/g,' অথবা ').replace(/\s+/g,' ').trim():'';}
    function setActiveCard(card){document.querySelectorAll('#grid > .card.teacher-active').forEach(e=>e.classList.remove('teacher-active'));if(!card)return;card.classList.add('teacher-active');const r=card.getBoundingClientRect();if(r.top<50||r.bottom>innerHeight-50)card.scrollIntoView({behavior:'smooth',block:'center'});}
    function findVoice(lang){const list=getVoices(),wanted=lang.toLowerCase(),exact=list.find(v=>String(v.lang||'').toLowerCase()===wanted);if(exact)return exact;const prefix=wanted.split('-')[0];return list.find(v=>String(v.lang||'').toLowerCase().split('-')[0]===prefix)||null;}
    function waitForVoices(timeout=VOICE_WAIT_MS){return new Promise(resolve=>{const s=synth();if(!s){resolve([]);return;}const initial=s.getVoices();if(initial.length){resolve(initial);return;}let done=false;const finish=()=>{if(done)return;done=true;try{s.removeEventListener('voiceschanged',finish);}catch(e){}resolve(s.getVoices());};try{s.addEventListener('voiceschanged',finish);}catch(e){s.onvoiceschanged=finish;}setTimeout(finish,timeout);});}
    function speakOnce(text,lang,voice){return new Promise(async resolve=>{const s=synth();if(!s||!text){resolve(false);return;}let started=false,settled=false;const u=new SpeechSynthesisUtterance(text);u.lang=voice?.lang||lang;u.rate=lang.startsWith('ja')?.9:.95;if(voice)u.voice=voice;u.onstart=()=>{started=true;};u.onend=()=>{if(settled)return;settled=true;resolve(started);};u.onerror=()=>{if(settled)return;settled=true;resolve(false);};try{s.resume();s.speak(u);}catch(e){resolve(false);}setTimeout(()=>{if(!started&&!settled){try{s.cancel();}catch(e){}settled=true;resolve(false);}},3500);});}
    async function speak(text,lang){if(!synth()||!text)return false;await waitForVoices();if(state==='stopped'||state==='paused')return false;let voice=findVoice(lang);const locales=lang.startsWith('bn')?[voice?.lang||'bn-BD','bn','bn-IN']:[voice?.lang||lang];for(const locale of [...new Set(locales)]){if(state==='stopped'||state==='paused')return false;voice=findVoice(lang);const ok=await speakOnce(text,locale,voice);if(ok)return true;await waitForVoices(800);try{synth().resume();}catch(e){}}return false;}
    function scheduleBangla(delay,id){clearTimeout(waitTimer);state='waiting';waitRemaining=delay;waitStartedAt=Date.now();updateUI();waitTimer=setTimeout(()=>{if(id!==runId||state==='stopped'||state==='paused')return;waitTimer=null;waitRemaining=0;speakBangla(id);},delay);}
    async function speakJapanese(){if(state==='stopped')return;const id=runId,card=cards[index];if(!card)return finish();setActiveCard(card);state='japanese';updateUI();const ok=await speak(japaneseText(card),'ja-JP');if(id!==runId||state==='stopped'||state==='paused')return;scheduleBangla(WAIT_MS,id);}
    async function speakBangla(id){if(id!==runId||state==='stopped'||state==='paused')return;const card=cards[index];if(!card)return finish();const text=banglaText(card);state='bangla';updateUI();if(!text)return;const ok=await speak(text,'bn-BD');if(id!==runId||state==='stopped'||state==='paused')return;if(!ok){console.warn('Teacher Mode: Bengali TTS unavailable; keeping current card and retrying.');setTimeout(()=>{if(id===runId&&state==='bangla')speakBangla(id);},RETRY_MS);return;}index++;if(index>=cards.length)finish();else speakJapanese();}
    function start(){const s=synth();runId++;try{s?.cancel();s?.resume();s?.getVoices();}catch(e){}clearTimeout(waitTimer);cards=getCurrentCards();if(!cards.length)return;index=0;pausedPhase=null;pausedSpeech=false;waitRemaining=WAIT_MS;state='japanese';speakJapanese();}
    function pause(){if(state==='stopped'||state==='paused')return;pausedPhase=state;if(state==='waiting'){waitRemaining=Math.max(0,waitRemaining-(Date.now()-waitStartedAt));clearTimeout(waitTimer);waitTimer=null;}else if(synth()?.speaking){try{synth().pause();}catch(e){}pausedSpeech=true;}state='paused';updateUI();}
    function resume(){if(state!=='paused')return;state=pausedPhase||'japanese';if(pausedSpeech&&synth()?.paused){pausedSpeech=false;try{synth().resume();}catch(e){}}else if(state==='waiting')scheduleBangla(waitRemaining||WAIT_MS,runId);else if(state==='japanese')speakJapanese();else if(state==='bangla')speakBangla(runId);else updateUI();}
    function stop(){runId++;try{synth()?.cancel();}catch(e){}clearTimeout(waitTimer);waitTimer=null;state='stopped';pausedPhase=null;pausedSpeech=false;index=0;waitRemaining=WAIT_MS;document.querySelectorAll('#grid > .card.teacher-active').forEach(e=>e.classList.remove('teacher-active'));updateUI();}
    function finish(){stop();}
    function updateButton(b){if(state==='stopped'){b.innerHTML=labelIcon('',ICONS.play);b.setAttribute('aria-label','Start Teacher Mode from first card');b.title='Start Teacher Mode';}else if(state==='paused'){b.innerHTML=labelIcon('',ICONS.play);b.setAttribute('aria-label','Resume Teacher Mode');b.title='Resume Teacher Mode';}else{b.innerHTML=labelIcon('',ICONS.pause);b.setAttribute('aria-label','Pause Teacher Mode');b.title='Pause Teacher Mode';}}
    function updateUI(){document.querySelectorAll('.teacher-mode-btn').forEach(updateButton);document.querySelectorAll('.teacher-stop-btn').forEach(b=>{b.innerHTML=labelIcon('',ICONS.stop);b.setAttribute('aria-label','Stop Teacher Mode');b.title='Stop Teacher Mode';b.disabled=state==='stopped';});}
    function createButton(cls){const b=document.createElement('button');b.type='button';b.className=cls;return b;}
    function addToolbarControls(){const toolbar=document.querySelector('.toolbar');if(!toolbar||document.querySelector('.teacher-mode-field'))return;const field=document.createElement('div');field.className='field teacher-mode-field';const startBtn=createButton('teacher-mode-btn'),stopBtn=createButton('teacher-stop-btn');field.append(startBtn,stopBtn);toolbar.appendChild(field);startBtn.addEventListener('click',()=>state==='stopped'?start():state==='paused'?resume():pause());stopBtn.addEventListener('click',stop);}
    function addMobileControls(){const bar=document.querySelector('.mobile-bottom-controls');if(!bar||bar.querySelector('.teacher-mode-btn'))return;const startBtn=createButton('teacher-mode-btn'),stopBtn=createButton('teacher-stop-btn');bar.append(startBtn,stopBtn);startBtn.addEventListener('click',()=>state==='stopped'?start():state==='paused'?resume():pause());stopBtn.addEventListener('click',stop);}
    function init(){if(!document.getElementById('grid'))return;addToolbarControls();addMobileControls();updateUI();try{synth()?.getVoices();synth()?.addEventListener?.('voiceschanged',()=>synth().getVoices());}catch(e){}}
    if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
