// =====================================================
// Lessons page controls
// =====================================================
(function () {
    let showBack = false, showBackRomaji = true, observer = null, resetButton = null, mobileQuery = null;
    const ICONS={
      screenOn:'<svg class="lesson-control-svg" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"></path></svg>',
      screenOff:'<svg class="lesson-control-svg" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 15.5A8 8 0 1 1 8.5 4 8 8 0 0 0 20 15.5Z"></path></svg>',
      romaji:'<svg class="lesson-control-svg" viewBox="0 0 24 24" aria-hidden="true"><path d="M4 5h7M7.5 5v14M4 19h7M14 5l6 14M20 5l-6 14"></path></svg>',
      flip:'<svg class="lesson-control-svg" viewBox="0 0 24 24" aria-hidden="true"><path d="M4 7h15l-3-3M20 17H5l3 3M19 7l-3-3M5 17l3 3"></path></svg>',
      shuffle:'<svg class="lesson-control-svg" viewBox="0 0 24 24" aria-hidden="true"><path d="M4 7h3c4 0 6 10 10 10h3M17 14l3 3-3 3M4 17h3c1.5 0 2.5-1.5 3.5-3M14 10c1-1.5 2-3 3-3h3M17 4l3 3-3 3"></path></svg>',
      reset:'<svg class="lesson-control-svg" viewBox="0 0 24 24" aria-hidden="true"><path d="M20 11a8 8 0 0 0-14.7-4L3 10M3 5v5h5M4 13a8 8 0 0 0 14.7 4L21 14M21 19v-5h-5"></path></svg>'
    };
    const labelledIcon=(label,icon)=>'<span class="lesson-control-text">'+label+'</span>'+icon;
    const getCards=()=>Array.from(document.querySelectorAll('#grid .card'));
    function updateScreenUI(){const b=document.getElementById('screenWakeToggle');if(!b)return;const on=b.getAttribute('aria-pressed')==='true';b.innerHTML=labelledIcon('Screen',on?ICONS.screenOn:ICONS.screenOff);b.setAttribute('aria-label',on?'Screen Always On':'Normal screen timeout');b.title=on?'Screen Always On':'Normal screen timeout';}
    function updateSwitchUI(){const d=document.getElementById('direction');if(!d)return;d.classList.toggle('right',showBack);d.setAttribute('aria-pressed',String(showBack));d.setAttribute('aria-label',showBack?'Show all cards Front':'Show all cards Back');d.title=showBack?'Show Front':'Show Back';d.innerHTML=labelledIcon('Front / Back',ICONS.flip);}
    function updateRomajiUI(){const b=document.getElementById('backRomajiToggle');if(!b)return;b.innerHTML=labelledIcon('Romaji',ICONS.romaji);b.setAttribute('aria-pressed',String(showBackRomaji));b.setAttribute('aria-label',showBackRomaji?'Romaji: ON':'Romaji: OFF');b.title=showBackRomaji?'Romaji: ON':'Romaji: OFF';}
    function applyRomajiVisibility(){getCards().forEach(c=>c.querySelectorAll('.romaji').forEach(r=>r.style.display=showBackRomaji?'':'none'));}
    function applyCardState(){getCards().forEach(c=>c.classList.toggle('flipped',showBack));applyRomajiVisibility();}
    function toggleAllCards(){showBack=!showBack;updateSwitchUI();applyCardState();}
    function toggleBackRomaji(){showBackRomaji=!showBackRomaji;updateRomajiUI();applyRomajiVisibility();}
    function resetLessons(){showBack=false;updateSwitchUI();applyCardState();window.scrollTo({top:0,left:0,behavior:'smooth'});}
    function createResetButton(){if(resetButton&&document.contains(resetButton))return resetButton;resetButton=document.createElement('button');resetButton.type='button';resetButton.id='lessonResetBtn';resetButton.className='lesson-reset-btn';resetButton.innerHTML=labelledIcon('Reset',ICONS.reset);resetButton.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();resetLessons();});return resetButton;}
    const getField=e=>e?e.closest('.field'):null;
    function ensureScreenField(){const t=document.querySelector('.toolbar'),b=document.getElementById('screenWakeToggle');if(!t||!b)return;let f=getField(b);if(!f){f=document.createElement('div');f.id='screenWakeField';f.className='field screen-wake-field';t.appendChild(f);f.appendChild(b);}updateScreenUI();}
    function ensureDesktopOrder(){const t=document.querySelector('.toolbar'),r=document.getElementById('backRomajiToggle'),d=document.getElementById('direction'),s=document.getElementById('shuffleBtn');if(!t||!r||!d||!s)return;const rf=getField(r),df=getField(d),sf=getField(s),screenField=getField(document.getElementById('screenWakeToggle'));if(!rf||!df||!sf)return;const reset=createResetButton();let resetField=document.getElementById('lessonResetField');if(!resetField){resetField=document.createElement('div');resetField.id='lessonResetField';resetField.className='field lesson-reset-field';resetField.appendChild(reset);}/* Screen is deliberately PC-hidden; Teacher/Stop occupy its toolbar slot. */if(screenField)screenField.style.display='none';t.appendChild(rf);t.appendChild(df);t.appendChild(sf);t.appendChild(resetField);updateRomajiUI();updateSwitchUI();s.innerHTML=labelledIcon('Shuffle',ICONS.shuffle);s.setAttribute('aria-label','Shuffle');s.title='Shuffle';}
    function ensureMobileControls(){const s=document.getElementById('shuffleBtn'),r=document.getElementById('backRomajiToggle'),d=document.getElementById('direction');if(!s||!r||!d)return;let bar=document.querySelector('.mobile-bottom-controls');if(!bar){bar=document.createElement('div');bar.className='mobile-bottom-controls';bar.setAttribute('aria-label','Lesson controls');document.body.appendChild(bar);}bar.appendChild(r);bar.appendChild(d);bar.appendChild(s);bar.appendChild(createResetButton());s.innerHTML=ICONS.shuffle;s.setAttribute('aria-label','Shuffle');s.title='Shuffle';updateRomajiUI();updateSwitchUI();}
    function syncResponsiveControls(){const mobile=mobileQuery?mobileQuery.matches:matchMedia('(max-width:520px)').matches;if(mobile){const f=getField(document.getElementById('screenWakeToggle'));if(f)f.style.display='';ensureMobileControls();}else ensureDesktopOrder();}
    function init(){const d=document.getElementById('direction'),grid=document.getElementById('grid'),r=document.getElementById('backRomajiToggle');if(!d||!grid)return;d.onclick=e=>{e.preventDefault();e.stopPropagation();toggleAllCards();};if(r)r.onclick=e=>{e.preventDefault();e.stopPropagation();toggleBackRomaji();};mobileQuery=matchMedia('(max-width:520px)');if(mobileQuery.addEventListener)mobileQuery.addEventListener('change',syncResponsiveControls);else mobileQuery.addListener(syncResponsiveControls);updateSwitchUI();updateRomajiUI();applyCardState();ensureScreenField();syncResponsiveControls();if(observer)observer.disconnect();observer=new MutationObserver(()=>{if(showBack||!showBackRomaji)applyCardState();});observer.observe(grid,{childList:true});}
    if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
